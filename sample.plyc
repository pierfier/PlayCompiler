Func simpl(n){
     n
}

Func addNums( arg1, arg2, num3 ){
    let add_nums = arg1 + arg2 in 
    	add_nums
}

Func fib(n){
    if n < 2
    	then 1
	else ((fib (n - 1)) + (fib (n - 2)))
}

main = do
    print (addNums 1 2 4)
    print (fib 5)
